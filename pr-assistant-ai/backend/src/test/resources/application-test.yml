spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379

app:
  jwt:
    secret: test-secret-key-that-is-long-enough-for-hs512-algorithm-at-least-64-chars
    expiration-ms: 3600000
  github:
    token: ghp_test
    webhook-secret: test-webhook-secret
    api-base-url: https://api.github.com
  ai:
    provider: mock
    openai:
      api-key: sk-test
      model: gpt-4
  cors:
    allowed-origins: http://localhost:4200

logging:
  level:
    dev.prassistant: WARN
