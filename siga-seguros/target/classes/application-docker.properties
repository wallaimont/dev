# Docker / Production profile - PostgreSQL
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:sigaseguros}
spring.datasource.username=${DB_USERNAME:sigaseguros}
spring.datasource.password=${DB_PASSWORD:sigaseguros123}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.flyway.locations=classpath:db/migration
spring.thymeleaf.cache=true
logging.level.com.sigaseguros=INFO
